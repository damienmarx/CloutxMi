# ============================================================================
# CloutScape Production Environment Configuration
# ============================================================================
# This file contains the environment variables for production deployment.
# Ensure all values are set correctly before deploying to production.
#
# IMPORTANT: Never commit this file to version control. Use your deployment
# platform's secrets management (Railway, Cloudflare, etc.) instead.
# ============================================================================

# Frontend Configuration (Vite - baked into build)
VITE_OAUTH_PORTAL_URL=https://manus.im/oauth
VITE_APP_ID=cloutscape-org
VITE_SITE_URL=https://cloutscape.org

# Backend Configuration
NODE_ENV=production
PORT=8080

# OAuth Server
OAUTH_SERVER_URL=https://manus.im/oauth

# Database Configuration
# Format: mysql://username:password@host:port/database
DATABASE_URL=mysql://clout_user:clout_pass@db:3306/cloutscape

# Security Keys (Generate strong random strings)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# Database Root Password (for Docker MySQL)
DB_ROOT_PASSWORD=your-mysql-root-password

# Optional: Trust Wallet Configuration
TRUST_WALLET_ADDRESS=your-trust-wallet-address

# Optional: AWS S3 Configuration (if using file uploads)
# AWS_ACCESS_KEY_ID=your-aws-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret
# AWS_S3_BUCKET=your-bucket-name
# AWS_REGION=us-east-1

# Optional: LLM Configuration (if using AI features)
# OPENAI_API_KEY=your-openai-key

# ============================================================================
# DEPLOYMENT NOTES:
# ============================================================================
# 1. Replace all placeholder values with your actual production values
# 2. Use strong, random strings for JWT_SECRET and SESSION_SECRET
# 3. For Railway: Add these variables in the Railway dashboard
# 4. For VPS + Docker: Create a .env file with these values
# 5. For Cloudflare Pages: Add these in the environment variables section
# 6. NEVER share your JWT_SECRET or DATABASE_URL publicly
# ============================================================================
